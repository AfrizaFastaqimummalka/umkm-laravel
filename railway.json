{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "releaseCommand": "php artisan storage:link && php artisan migrate --force && php artisan db:seed --class=AdminSeeder --force",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}
